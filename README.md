# ctaMTX built using Flask

Web version of ctaMTX, specifically the second version.

## Usage

Run this in your terminal:

```python -r -requirements.txt```

And then just run the ``app.py`` and you're ready to go !
